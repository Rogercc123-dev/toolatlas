const fs=require('fs'),path=require('path');const DIST=path.join(__dirname,'..','dist');const urls=['https://toolatlas.online/'];for(const ent of fs.readdirSync(DIST,{withFileTypes:true})){if(ent.isDirectory()&&fs.existsSync(path.join(DIST,ent.name,'index.html')))urls.push(`https://toolatlas.online/${ent.name}/`)}const today=new Date().toISOString().slice(0,10);const xml='<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+urls.map(u=>`<url><loc>${u}</loc><lastmod>${today}</lastmod></url>`).join('')+'</urlset>';fs.writeFileSync(path.join(DIST,'sitemap.xml'),xml);fs.writeFileSync(path.join(DIST,'robots.txt'),'User-agent: *\nAllow: /\nSitemap: https://toolatlas.online/sitemap.xml\n');console.log(`Sitemap contains ${urls.length} URLs.`);
